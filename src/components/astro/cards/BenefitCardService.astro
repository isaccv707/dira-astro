---
import { Icon } from "astro-icon/components";

interface Props {
  title: string;
  description: string;
  icon: string;
}

const { title, description, icon } = Astro.props;

const KNOWN_SETS = ["lucide:", "tabler:", "logos:"];
const isAstroIcon = icon && !icon.startsWith("http") && KNOWN_SETS.some((s) => icon.startsWith(s));
const isImgIcon = icon && icon.startsWith("http");
---

<div
  class="group relative overflow-hidden rounded-clinical-lg border border-ui-border bg-white p-6 shadow-xs transition-all duration-300 hover:-translate-y-1 hover:shadow-sm"
>
  <div class="relative flex flex-col gap-4">
    <div
      class="flex h-12 w-12 items-center justify-center rounded-clinical-sm bg-white shadow-xs ring-1 ring-black/5 transition-transform duration-300 group-hover:scale-110"
    >
      {
        isAstroIcon ? (
          <Icon name={icon} class="h-7 w-7 text-green-primary" />
        ) : isImgIcon ? (
          <img src={icon} alt={title} class="h-7 w-7 object-contain" />
        ) : (
          <div class="h-7 w-7 bg-green-primary/10 rounded-clinical-sm" />
        )
      }
    </div>

    <div>
      <h3 class="text-base font-bold tracking-tight text-green-primary">
        {title}
      </h3>
      <p class="mt-2 text-sm leading-relaxed text-grey-custom">
        {description}
      </p>
    </div>
  </div>

  <div
    class="absolute bottom-0 left-0 h-1 w-0 bg-green-primary transition-all duration-500 group-hover:w-full"
  >
  </div>
</div>
